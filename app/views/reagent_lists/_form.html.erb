<%= form_with(model: [@program, @analyte, reagent_list], local: true) do |form| %>
  <% if reagent_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reagent_list.errors.count, "error") %> prohibited this reagent_list from being saved:</h2>

      <ul>
      <% reagent_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :reagent_list_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
