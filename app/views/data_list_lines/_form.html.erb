<%= form_with(model: [@data_list, data_list_line], local: true) do |form| %>
  <% if data_list_line.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(data_list_line.errors.count, "error") %> prohibited this data_list_line from being saved:</h2>

      <ul>
      <% data_list_line.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :value %>
    <%= form.number_field :value, id: :data_list_line_value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
