<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @unit_list.name %>
</p>

<%= link_to 'Edit', params[:program_id] != nil ? edit_program_analyte_unit_list_path(params[:program_id], params[:analyte_id], @unit_list) : edit_unit_list_path(@unit_list) %> |
<%= link_to "back", params[:program_id] != nil ? program_analytes_path(params[:program_id]) : unit_lists_path %>
