<%= form_with(model: [@program, @analyte, unit_list], local: true) do |form| %>
  <% if unit_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(unit_list.errors.count, "error") %> prohibited this unit_list from being saved:</h2>

      <ul>
      <% unit_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :unit_list_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
