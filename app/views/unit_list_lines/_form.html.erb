<%= form_with(model: [@unit_list, unit_list_line], local: true) do |form| %>
  <% if unit_list_line.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(unit_list_line.errors.count, "error") %> prohibited this unit_list_line from being saved:</h2>

      <ul>
      <% unit_list_line.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :unit_list_line_name %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.number_field :value, id: :unit_list_line_value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
