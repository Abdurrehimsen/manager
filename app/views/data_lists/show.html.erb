<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @data_list.name %>
</p>

<%= link_to 'Edit', params[:program_id] != nil ? edit_program_analyte_data_list_path(params[:program_id], params[:analyte_id], @data_list) : edit_data_list_path(@data_list) %> |
<%= link_to "back", params[:program_id] != nil ? program_analytes_path(params[:program_id]) : data_lists_path %>
