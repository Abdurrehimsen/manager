<%= form_with(model: [@program, analyte], local: true) do |form| %>
  <% if analyte.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(analyte.errors.count, "error") %> prohibited this analyte from being saved:</h2>

      <ul>
      <% analyte.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :analyte_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
